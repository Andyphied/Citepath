"use client";

import { PageStub } from "@/components/page-stub";

export default function AdminPage() {
  return (
    <PageStub
      title="Admin"
      storyId="UI-006"
      description="Workspace operations dashboard: documents, jobs, questions, and usage."
    />
  );
}
